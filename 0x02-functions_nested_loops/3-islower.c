#include "main.h"

/**
 * _islower - function that checks for lowercase character
 *
 * @c: character to innput
 *
 * Return: 1 if c is lowercase
 * otherwise 0
 */
int _islower(__attribute__((unused)) int c)
{
	int i;

	for (i = 'a'; i <= 'z';)

	{
		return (1);
	}
	return (0);
}
